# REALMS — Deutsche Fassung

Die deutsche Ausgabe des REALMS-Manuskripts liegt in diesem Ordner. PDF aus den Markdown-Dateien erzeugen:

```bash
./build-pdf-de.sh
```

(aus dem übergeordneten Verzeichnis `realms.txt/`). Das Skript erzeugt `de/manuscript-combined.md` und `de/manuscript.pdf`.
