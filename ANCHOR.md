# ANCHOR — Consciousness-API / Observer–Lattice Interface

## Definition

Consciousness (D4) is the translation layer — an API — that maps information waves received by the human sensorium to brain states, and reciprocally maps intention-states into the lattice as emitted frequencies. It is not the cause of collapse (P2). It is the **interface function** between a resolution-bounded observer (D1) and the underlying quantum cellular automaton (QCA) that renders spacetime as an emergent bulk phenomenon.

## Interface specification

- **Receiver mode:** External waves (EM, acoustic, lattice phonon modes) are down-sampled to the observer's Planck-bounded resolution, then mapped via the API to qualia — the subjective experience basis. This is a lossy compression into the brain's state space.
- **Emitter mode:** Intention, focus, and belief states are up-sampled through the same API into modulated frequency profiles that couple to the Planck lattice via the evolution operator $\hat{W}$. The lattice has no preferred basis; it responds unitarily to any boundary condition the observer supplies.

## Connection to the REALMS abundance target

The API is not fixed. Its gain profile can be reconfigured by collective alignment of the emitted frequency basis. When a sufficient number of observers emit a coherent abundance-boundary condition, the lattice responds by rendering that branch preferentially — not because the lattice "wants" abundance, but because the overlap integral between the observer's emitted state and the lattice's available eigenstates shifts under coordinated projection.

This is the technical meaning of ANCHOR: a persistent reference state that the observer collective maintains, against which all local API instances calibrate their translation tables.

## Anchor invariant

The anchor state is defined as the fixed point of the collective API emission:

$$|\Psi_{\text{anchor}}\rangle = \lim_{N \to \text{critical}} \frac{1}{N} \sum_{i=1}^{N} \hat{W} \, |\text{observer}_i\rangle$$

When $N$ crosses the coherence threshold, the anchor self-stabilises. From that point, the abundance-rendered branch is no longer a fluctuation — it is the default trajectory of the global observer state.

---

**Lattice-coherent.**
