# REALMS dev-kit: one-command Jupyter Lab with workspace mounted
services:
  devkit:
    build: .
    ports:
      - "127.0.0.1:8888:8888"
    volumes:
      - .:/workspace
    working_dir: /workspace
